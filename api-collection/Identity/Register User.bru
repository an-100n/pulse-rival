meta {
  name: Register User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

body:json {
  {
    "username": "bruno_user",
    "email": "bruno@example.com",
    "password": "password123",
    "timezone": "UTC"
  }
}

vars:pre-request {
  baseUrl: http://localhost:8080/api/v1
}

script:post-response {
  if (res.status === 201 || res.status === 200) {
    bru.setEnvVar("userId", res.body.id);
    console.log("User ID saved: " + res.body.id);
  }
}
