spring.application.name=PulseRival-API

# --- Database Configuration (Postgres 16) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/pulserival
spring.datasource.username=admin
spring.datasource.password=berlin2026
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Redis Configuration ---
spring.data.redis.host=localhost
spring.data.redis.port=6379

# --- Docker Compose Integration ---
# We disable this to manually control the container lifecycle via 'docker compose up'
spring.docker.compose.enabled=false

security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970